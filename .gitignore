# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
# playwright e2e artifacts (v10.3.0)
/test-results/
/playwright-report/
/playwright/.cache/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# database + user runtime content
/data/*.db
/data/*.db-wal
/data/*.db-shm
/data/storage/
/data/composed/
/data/exports/
/data/media/
/data/covers/
/.dev-logs/

# v12.129 audit P0-2: prevent accidental commit of secrets / local runtime junk
# .env*.local only matched forward suffixes; key-backup files like .env.local.bak-* slipped through.
*.bak
.env*.bak*
/*.db
/*.db-wal
/*.db-shm
.kokoro-venv/
*-venv/
.venv/
/projects.db
/projects.db-wal
/projects.db-shm

# user uploads
/public/uploads/
/public/generated/
/public/temp/

# dev / debug artifacts
/dev.log
/debug-frontend.js
/test-api.js
/test-api2.js
/test-api3.js
/test-api.sh
/test-minimax-video.js
/test-frontend-issue.html
/test-login.html
/vitest.config.ts.json

# IDE / agent workspace
/.claude/

# hero assets: 已改为「提交的固定素材」(v9.5.0 起 = 用户提供片段, 入库版本控制)
# 如需重新 AI 生成可跑 `npm run generate:hero`(会覆盖本地文件)
# /public/hero-cover.jpg
# /public/hero-loop.mp4

# v4.x: 测试隔离 DB (vitest 专用, 不入库)
data/qfmj.test.db
data/qfmj.test.db-*
data/qfmj.db.bak-*

# v12.321:宣传片工程的重型产物不入库 —— 渲染产物可重生,成片素材是从
# videos/wc-promo 复制来的副本,重复入库只是让仓库变胖。
videos/wind-comic-promo/renders/
# v12.336 例外:assets/audio/ **入库**,不在忽略之列。
# renders/ 能从源码确定性重建,而配乐/配音是**付费且不可复现**的产物 ——
# MiniMax 订阅每天只有 3 条生成额度,且同一 prompt 每次出的曲子都不一样,
# 丢了就再也拿不回这一条。这正是版本控制该保管的东西。
videos/wind-comic-promo/assets/footage-*.mp4
videos/wind-comic-promo/assets/*.jpg
# 这几张是 assets/ 下产品截图的副本(product-hero 就是 banner.png),别重复入库
videos/wind-comic-promo/assets/product-hero.png
videos/wind-comic-promo/assets/publish-gate.png
videos/wind-comic-promo/assets/final-film.png
videos/wind-comic-promo/assets/full-chain-control.png
videos/wind-comic-promo/assets/cinema-timeline.png
videos/wind-comic-promo/assets/voice/
videos/wind-comic-promo/capture/
videos/wind-comic-promo/compositions/frames/assets/
.tools/
